[project]
name = "nucleushub"
version = "0.1.0"
description = "Nucleus Hub JupyterHub deployment project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyterhub>=5.4.4",
    "oauthenticator>=17.4.0",
]
